<h1><?= $title ?></h1>

<div class="mb-3">
    <a href="<?= PUBLIC_PATH ?>usuarios" class="btn btn-light">
        <i class="ti ti-arrow-left me-1"></i> Volver
    </a>
</div>

<div class="card">
    <div class="card-body">
        <form method="post">
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label">Nombre <span class="text-danger">*</span></label>
                    <input type="text" name="usuario[nombre]" class="form-control" value="<?= $usuario->nombre ?>"
                        required>
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label">Email <span class="text-danger">*</span></label>
                    <input type="email" name="usuario[email]" class="form-control" value="<?= $usuario->email ?>"
                        required>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label">Teléfono</label>
                    <input type="text" name="usuario[telefono]" class="form-control"
                        value="<?= $usuario->telefono ?? '' ?>" placeholder="555-123-4567">
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label">Nueva Contraseña</label>
                    <input type="password" name="usuario[password]" class="form-control"
                        placeholder="Dejar en blanco para no cambiar">
                    <small class="text-muted">Solo completar si deseas cambiar la contraseña</small>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label">Rol</label>
                    <select name="usuario[rol_id]" class="form-select">
                        <option value="1" <?= $usuario->rol_id == 1 ? 'selected' : '' ?>>Administrador</option>
                        <option value="2" <?= $usuario->rol_id == 2 ? 'selected' : '' ?>>Usuario</option>
                        <option value="3" <?= $usuario->rol_id == 3 ? 'selected' : '' ?>>Invitado</option>
                    </select>
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label">Estado</label>
                    <select name="usuario[activo]" class="form-select">
                        <option value="1" <?= $usuario->activo == 1 ? 'selected' : '' ?>>Activo</option>
                        <option value="0" <?= $usuario->activo == 0 ? 'selected' : '' ?>>Inactivo</option>
                    </select>
                </div>
            </div>

            <div class="text-end">
                <button type="submit" class="btn btn-primary">
                    <i class="ti ti-device-floppy me-1"></i> Actualizar
                </button>
            </div>
        </form>
    </div>
</div>