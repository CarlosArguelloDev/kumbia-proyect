<h1><?= $title ?></h1>

<div class="d-flex justify-content-between mb-3">
    <div>
        <?= Bform::btn_volver('javascript:history.back()', 'Volver') ?>
    </div>
    <div>
        <?= Bform::btn_agregar("usuarios/crear", "Agregar Usuario") ?>
    </div>
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Email</th>
                        <th>Teléfono</th>
                        <th>Activo</th>
                        <th>Fecha Registro</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($usuarios as $u): ?>
                        <tr>
                            <td><?= $u->id ?></td>
                            <td><?= $u->nombre ?></td>
                            <td><?= $u->email ?></td>
                            <td><?= $u->telefono ?? '-' ?></td>
                            <td>
                                <?php if ($u->activo == 1): ?>
                                    <span class="badge bg-success">Activo</span>
                                <?php else: ?>
                                    <span class="badge bg-danger">Inactivo</span>
                                <?php endif; ?>
                            </td>
                            <td>
                                <small><?= date('d/m/Y', strtotime($u->created_at)) ?></small>
                            </td>
                            <td>
                                <?= Bform::grupo_acciones($u->id, 'usuarios', [
                                    'ver' => false,
                                    'mensaje_eliminar' => '¿Estás seguro de eliminar este usuario?'
                                ]) ?>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>