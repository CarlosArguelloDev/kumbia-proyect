<div class="card shadow-lg">
    <div class="card-body p-5">
        <div class="text-center mb-4">
            <i class="ti ti-user-plus display-4 text-primary"></i>
            <h3 class="mt-3 mb-2"><?= $title ?></h3>
            <p class="text-muted">Crea tu cuenta para comenzar</p>
        </div>

        <?= Form::open() ?>
        <div class="mb-3">
            <label class="form-label">Nombre Completo</label>
            <input type="text" name="usuario[nombre]" class="form-control" placeholder="Juan Pérez" required autofocus>
        </div>

        <div class="mb-3">
            <label class="form-label">Email</label>
            <input type="email" name="usuario[email]" class="form-control" placeholder="correo@ejemplo.com" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Teléfono (opcional)</label>
            <input type="text" name="usuario[telefono]" class="form-control" placeholder="4421234567">
        </div>

        <div class="mb-3">
            <label class="form-label">Contraseña</label>
            <input type="password" name="usuario[password]" class="form-control" placeholder="••••••••" required>
        </div>

        <div class="d-grid mb-3">
            <button type="submit" class="btn btn-primary btn-lg">
                <i class="ti ti-user-plus me-1"></i> Registrarse
            </button>
        </div>
        <?= Form::close() ?>

        <div class="text-center">
            <p class="text-muted mb-0">
                ¿Ya tienes cuenta?
                <a href="<?= PUBLIC_PATH ?>auth/login" class="link-primary fw-bold">
                    Inicia sesión aquí
                </a>
            </p>
        </div>
    </div>
</div>

<style>
    .auth-main {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background-image: url('<?= PUBLIC_PATH ?>storage/banner.png');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
    }

    .auth-main::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.4);
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
        z-index: 1;
    }

    .auth-wrapper {
        position: relative;
        width: 100%;
        max-width: 480px;
        padding: 20px;
        z-index: 2;
    }

    .card {
        border: none;
        border-radius: 15px;
        background: rgba(255, 255, 255, 0.98);
    }

    .card-body {
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
    }
</style>