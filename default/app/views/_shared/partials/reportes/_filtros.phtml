<!-- Sección de Filtros -->
<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h6 class="mb-0">
            <i class="ti ti-filter"></i> Filtros
            <?php
            $filtrosActivos = 0;
            if (!empty($filtros['estado_id']))
                $filtrosActivos++;
            if (!empty($filtros['prioridad_id']))
                $filtrosActivos++;
            if (!empty($filtros['fecha_desde']))
                $filtrosActivos++;
            if (!empty($filtros['fecha_hasta']))
                $filtrosActivos++;
            if (!empty($filtros['busqueda']))
                $filtrosActivos++;
            if ($filtrosActivos > 0): ?>
                <span class="badge bg-primary"><?= $filtrosActivos ?></span>
            <?php endif; ?>
        </h6>
        <button class="btn btn-sm btn-link" type="button" data-bs-toggle="collapse" data-bs-target="#filtrosCollapse">
            <i class="ti ti-chevron-down"></i>
        </button>
    </div>
    <div class="collapse <?= $filtrosActivos > 0 ? 'show' : '' ?>" id="filtrosCollapse">
        <div class="card-body">
            <form method="get" action="<?= $action_url ?>" id="formFiltros">
                <div class="row g-3">
                    <!-- Búsqueda de texto -->
                    <div class="col-md-12">
                        <label class="form-label">
                            <i class="ti ti-search"></i> Buscar
                        </label>
                        <input type="text" name="busqueda" class="form-control"
                            placeholder="Buscar en título, descripción o dirección..."
                            value="<?= htmlspecialchars($filtros['busqueda'] ?? '') ?>">
                    </div>

                    <!-- Estado -->
                    <div class="col-md-3">
                        <label class="form-label">
                            <i class="ti ti-flag"></i> Estado
                        </label>
                        <select name="estado_id" class="form-select">
                            <option value="">Todos</option>
                            <?php foreach ($estados as $estado): ?>
                                <option value="<?= $estado->id ?>" <?= ($filtros['estado_id'] == $estado->id) ? 'selected' : '' ?>>
                                    <?= $estado->nombre ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>

                    <!-- Prioridad -->
                    <div class="col-md-3">
                        <label class="form-label">
                            <i class="ti ti-alert-triangle"></i> Prioridad
                        </label>
                        <select name="prioridad_id" class="form-select">
                            <option value="">Todas</option>
                            <?php foreach ($prioridades as $prioridad): ?>
                                <option value="<?= $prioridad->id ?>" <?= ($filtros['prioridad_id'] == $prioridad->id) ? 'selected' : '' ?>>
                                    <?= $prioridad->nombre ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>

                    <!-- Fecha desde -->
                    <div class="col-md-3">
                        <label class="form-label">
                            <i class="ti ti-calendar"></i> Desde
                        </label>
                        <input type="date" name="fecha_desde" class="form-control"
                            value="<?= htmlspecialchars($filtros['fecha_desde'] ?? '') ?>">
                    </div>

                    <!-- Fecha hasta -->
                    <div class="col-md-3">
                        <label class="form-label">
                            <i class="ti ti-calendar"></i> Hasta
                        </label>
                        <input type="date" name="fecha_hasta" class="form-control"
                            value="<?= htmlspecialchars($filtros['fecha_hasta'] ?? '') ?>">
                    </div>

                    <!-- Botones -->
                    <div class="col-12">
                        <button type="submit" class="btn btn-primary">
                            <i class="ti ti-filter"></i> Aplicar filtros
                        </button>
                        <a href="<?= $action_url ?>" class="btn btn-outline-secondary">
                            <i class="ti ti-x"></i> Limpiar filtros
                        </a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>