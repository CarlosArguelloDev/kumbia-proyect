<!-- Sección de Filtros para Usuarios -->
<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h6 class="mb-0">
            <i class="ti ti-filter"></i> Filtros
            <?php
            $filtrosActivos = 0;
            if (!empty($filtros['busqueda']))
                $filtrosActivos++;
            if (isset($filtros['activo']) && $filtros['activo'] !== '' && $filtros['activo'] !== null)
                $filtrosActivos++;
            if (!empty($filtros['fecha_desde']))
                $filtrosActivos++;
            if (!empty($filtros['fecha_hasta']))
                $filtrosActivos++;
            if ($filtrosActivos > 0): ?>
                <span class="badge bg-primary"><?= $filtrosActivos ?></span>
            <?php endif; ?>
        </h6>
        <button class="btn btn-sm btn-link" type="button" data-bs-toggle="collapse" data-bs-target="#filtrosCollapse">
            <i class="ti ti-chevron-down"></i>
        </button>
    </div>
    <div class="collapse <?= $filtrosActivos > 0 ? 'show' : '' ?>" id="filtrosCollapse">
        <div class="card-body">
            <form method="get" action="<?= PUBLIC_PATH ?>usuarios/index" id="formFiltros">
                <div class="row g-3">
                    <!-- Búsqueda de texto -->
                    <div class="col-md-6">
                        <label class="form-label">
                            <i class="ti ti-search"></i> Buscar
                        </label>
                        <input type="text" name="busqueda" class="form-control"
                            placeholder="Buscar por nombre o email..."
                            value="<?= htmlspecialchars($filtros['busqueda'] ?? '') ?>">
                    </div>

                    <!-- Estado Activo -->
                    <div class="col-md-2">
                        <label class="form-label">
                            <i class="ti ti-user-check"></i> Estado
                        </label>
                        <select name="activo" class="form-select">
                            <option value="">Todos</option>
                            <option value="1" <?= (isset($filtros['activo']) && $filtros['activo'] == '1') ? 'selected' : '' ?>>Activos</option>
                            <option value="0" <?= (isset($filtros['activo']) && $filtros['activo'] == '0') ? 'selected' : '' ?>>Inactivos</option>
                        </select>
                    </div>

                    <!-- Fecha desde -->
                    <div class="col-md-2">
                        <label class="form-label">
                            <i class="ti ti-calendar"></i> Desde
                        </label>
                        <input type="date" name="fecha_desde" class="form-control"
                            value="<?= htmlspecialchars($filtros['fecha_desde'] ?? '') ?>">
                    </div>

                    <!-- Fecha hasta -->
                    <div class="col-md-2">
                        <label class="form-label">
                            <i class="ti ti-calendar"></i> Hasta
                        </label>
                        <input type="date" name="fecha_hasta" class="form-control"
                            value="<?= htmlspecialchars($filtros['fecha_hasta'] ?? '') ?>">
                    </div>

                    <!-- Botones -->
                    <div class="col-12">
                        <button type="submit" class="btn btn-primary">
                            <i class="ti ti-filter"></i> Aplicar filtros
                        </button>
                        <a href="<?= PUBLIC_PATH ?>usuarios/index" class="btn btn-outline-secondary">
                            <i class="ti ti-x"></i> Limpiar filtros
                        </a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>