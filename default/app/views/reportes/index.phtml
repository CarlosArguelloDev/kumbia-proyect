<h1><?= $title ?></h1>
<div class="row">
    <div class="col-sm-12">
        <!-- Sección de Filtros -->
        <div class="card mb-3">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h6 class="mb-0">
                    <i class="ti ti-filter"></i> Filtros
                    <?php 
                    $filtrosActivos = 0;
                    if (!empty($filtros['estado_id'])) $filtrosActivos++;
                    if (!empty($filtros['prioridad_id'])) $filtrosActivos++;
                    if (!empty($filtros['fecha_desde'])) $filtrosActivos++;
                    if (!empty($filtros['fecha_hasta'])) $filtrosActivos++;
                    if (!empty($filtros['busqueda'])) $filtrosActivos++;
                    if ($filtrosActivos > 0): ?>
                        <span class="badge bg-primary"><?= $filtrosActivos ?></span>
                    <?php endif; ?>
                </h6>
                <button class="btn btn-sm btn-link" type="button" data-bs-toggle="collapse" data-bs-target="#filtrosCollapse">
                    <i class="ti ti-chevron-down"></i>
                </button>
            </div>
            <div class="collapse <?= $filtrosActivos > 0 ? 'show' : '' ?>" id="filtrosCollapse">
                <div class="card-body">
                    <form method="get" action="<?= PUBLIC_PATH ?>reportes/index" id="formFiltros">
                        <div class="row g-3">
                            <!-- Búsqueda de texto -->
                            <div class="col-md-12">
                                <label class="form-label">
                                    <i class="ti ti-search"></i> Buscar
                                </label>
                                <input type="text" name="busqueda" class="form-control" 
                                    placeholder="Buscar en título, descripción o dirección..." 
                                    value="<?= htmlspecialchars($filtros['busqueda'] ?? '') ?>">
                            </div>

                            <!-- Estado -->
                            <div class="col-md-3">
                                <label class="form-label">
                                    <i class="ti ti-flag"></i> Estado
                                </label>
                                <select name="estado_id" class="form-select">
                                    <option value="">Todos</option>
                                    <?php foreach ($estados as $estado): ?>
                                        <option value="<?= $estado->id ?>" 
                                            <?= ($filtros['estado_id'] == $estado->id) ? 'selected' : '' ?>>
                                            <?= $estado->nombre ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            </div>

                            <!-- Prioridad -->
                            <div class="col-md-3">
                                <label class="form-label">
                                    <i class="ti ti-alert-triangle"></i> Prioridad
                                </label>
                                <select name="prioridad_id" class="form-select">
                                    <option value="">Todas</option>
                                    <?php foreach ($prioridades as $prioridad): ?>
                                        <option value="<?= $prioridad->id ?>" 
                                            <?= ($filtros['prioridad_id'] == $prioridad->id) ? 'selected' : '' ?>>
                                            <?= $prioridad->nombre ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            </div>

                            <!-- Fecha desde -->
                            <div class="col-md-3">
                                <label class="form-label">
                                    <i class="ti ti-calendar"></i> Desde
                                </label>
                                <input type="date" name="fecha_desde" class="form-control" 
                                    value="<?= htmlspecialchars($filtros['fecha_desde'] ?? '') ?>">
                            </div>

                            <!-- Fecha hasta -->
                            <div class="col-md-3">
                                <label class="form-label">
                                    <i class="ti ti-calendar"></i> Hasta
                                </label>
                                <input type="date" name="fecha_hasta" class="form-control" 
                                    value="<?= htmlspecialchars($filtros['fecha_hasta'] ?? '') ?>">
                            </div>

                            <!-- Botones -->
                            <div class="col-12">
                                <button type="submit" class="btn btn-primary">
                                    <i class="ti ti-filter"></i> Aplicar filtros
                                </button>
                                <a href="<?= PUBLIC_PATH ?>reportes/index" class="btn btn-outline-secondary">
                                    <i class="ti ti-x"></i> Limpiar filtros
                                </a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <!-- Card de Reportes -->
        <div class="card">
            <div class="card-header">
                <h5><?= $subtitle ?></h5>
            </div>
            <div class="card-body">
                <!-- Contenidoend -->
                <?= Bform::btn_agregar("reportes/registrar", "Realizar reporte") ?>
                <br><br>

                <?php if (count($reportes) == 0): ?>
                    <div class="alert alert-info">
                        <i class="ti ti-info-circle"></i> No se encontraron reportes con los filtros aplicados.
                    </div>
                <?php endif; ?>

                <?php foreach ($reportes as $r): ?>
                    <div class="card overflow-hidden mb-3">
                        <div class="row g-0">
                            
                            <div class="col-md-4">
                                <img src="<?= PUBLIC_PATH ?>storage/reportes/<?= $r->id ?>.jpg"
                                    alt="Imagen Reporte <?= $r->id ?>" class="img-fluid w-100"
                                    style="object-fit: cover; max-height: 300px;">
                            </div>
            
                            <div class="col-md-8">
                                <div class="card-body">
                                    <!-- Titulo del reporte-->
                                    <h5 class="card-title">
                                        <?= $r->titulo ?>
                                    </h5>
                                    <!-- Badges -->
                                    <span class="badge bg-light-warning">
                                        <i class="ti ti-ruler-2"></i>
                                        Diámetro: <?= $r->diametro ?>cm
                                    </span>
                                    <?= Bform::badge_estado($r->getEstados()) ?>
                                    <?= Bform::badge_prioridad($r->getPrioridades()) ?>
                                    <!-- Atributos -->
                                    <p class="card-text"><?= $r->descripcion ?></p>
                                    <p class="card-text">
                                        <i class="ti ti-calendar-event"></i>
                                        <small class="text-muted">
                                            <?= date('m/d/Y', strtotime($r->created_at)) ?>
                                        </small>
                                        <i class="ti ti-map-pin ms-2"></i>
                                        <small class="text-muted">
                                            <?= $r->direccion ?>
                                        </small>
                                        <i class="ti ti-user"></i>
                                        <small class="text-muted">
                                            <?= $r->getUsuarios()->nombre ?>
                                        </small>
                                        <br><br>
                                        <!-- Links  -->
                                        <a href="<?= PUBLIC_PATH ?>reportes/ver/<?= $r->id ?>" class="card-link">Ver Más</a>
                                        <a href="#!" class="card-link"
                                            onclick="toggleComentarios(<?= $r->id ?>)">Comentarios</a>


                                    </p>
                                </div>
                            </div>
                            <div id="comentarios-<?= $r->id ?>" class="mt-2" style="display:none;">
                                
                                <h6><i class="ti ti-message-2"></i> Comentarios</h6>

                                <?php $comments = (new Comentarios)->find("reporte_id = {$r->id}", "order: created_at DESC"); ?>

                                <?php if (count($comments) > 0): ?>
                                    <?php foreach ($comments as $c): ?>
                                        <div class="border rounded p-3 mb-3">
                                            <div class="d-flex justify-content-between align-items-center mb-2">
                                                <strong><i class="ti ti-user"></i> <?= $c->getUsuarios()->nombre ?></strong>
                                                <small class="text-muted">
                                                    <i class="ti ti-clock"></i> <?= date('d/m/Y H:i', strtotime($c->created_at)) ?>
                                                </small>
                                            </div>
                                            <p class="mb-0"><?= $c->texto ?></p>
                                            <?php if ($c->publico): ?>
                                                <span class="badge bg-light-info mt-2"><i class="ti ti-eye"></i> Público</span>
                                            <?php endif; ?>
                                        </div>
                                    <?php endforeach; ?>
                                <?php else: ?>
                                    <p class="text-muted">No hay comentarios aún</p>
                                <?php endif; ?>

                                <hr>

                                <!-- Form -->
                                <h6 class="mb-2">Agregar Comentario</h6>
                                <form action="<?= PUBLIC_PATH ?>comentarios/crear/<?= $r->id ?>" method="post">

                                    <input type="hidden" name="comentario[reporte_id]" value="<?= $r->id ?>">

                                    <textarea name="comentario[texto]" class="form-control mb-2" rows="3"
                                        placeholder="Escribe un comentario..." required></textarea>

                                    <button class="btn btn-primary btn-sm">
                                        <i class="ti ti-send"></i> Enviar
                                    </button>
                                </form>
                            </div>


                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        </div>
    </div>
</div>


<script>
    function toggleComentarios(id) {
        let div = document.getElementById("comentarios-" + id);
        div.style.display = (div.style.display === "none") ? "block" : "none";
    }
</script>